name: Dump HTML (temporary, for debugging)

on:
  workflow_dispatch: {}

jobs:
  dump:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: pip install requests

      - name: Run dump script
        run: python dump.py

      - name: Upload HTML dumps as artifact
        uses: actions/upload-artifact@v4
        with:
          name: html-dumps
          path: |
            kepco.html
            nia.html
          retention-days: 1
